# Lexical Fuzzy Matching

Status: `pending`
Phase: 3
Depends on: phase-2 complete

## Objective

Implement Levenshtein-based string similarity to detect near-exact sentence repetitions.

## Requirements

_To be filled during grilling session._

## Implementation Notes

_To be filled during grilling session._

## Acceptance Criteria

- [ ] Similarity ratio computed between sentence pairs using `thefuzz`
- [ ] Both `fuzz.ratio()` and `fuzz.token_sort_ratio()` available
- [ ] Handles filler words and minor ASR transcription errors gracefully
