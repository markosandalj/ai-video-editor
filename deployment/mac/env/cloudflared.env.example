# Copy to .env.cloudflared in the repository root, fill it there, then chmod 0600.
# The real file is excluded by both .gitignore and .dockerignore.
# This file belongs only to cloudflared and must contain no worker/provider secrets.
TUNNEL_TOKEN=replace-with-cloudflare-tunnel-token
